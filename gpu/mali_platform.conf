# Copyright 2023 The ChromiumOS Authors
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

OSU_PROTECTED_MEMORY_HEAP_NAME=restricted_mtk_cma
PLATFORM_AGT_FREQUENCY_KHZ=13000

# To collect gpu.renderstages information on Mali GPU with Perfetto, build the
# mali-drivers package with "gpu_profiling" USE flag and uncomment the following
# line to enable the feature. Note that this may produce 7~10% CPU overhead.
# MALI_GPU_RENDERSTAGES_ENABLE=1
